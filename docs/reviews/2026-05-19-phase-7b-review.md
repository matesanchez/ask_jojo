# Phase 7b Review — Standalone Workstation Installer

**Date:** 2026-05-19
**Reviewer:** sub-agent (Opus, cold-context, read-only)
**Verdict:** PASS 12/12 items — no blockers

---

## Per-Item Checklist

1. `ops/installer/Build-JojoBotRelease.ps1` — **PASS** — 789 lines. Builds `.zip` with PyInstaller `--onedir` (primary) + `pip --target` (fallback), `npm run build`, NSSM bundling, end-user `Install-JojoBot.ps1` heredoc (lines 562–694), README.txt. Pure ASCII confirmed (0 non-ASCII bytes).
2. `ops/installer/Install-JojoBot.ps1` — **PASS** — 463 lines (developer bootstrap). End-user variant generated by Build script as embedded heredoc; registers Windows Service + opens browser to `/welcome`. ASCII clean.
3. `ops/installer/Install-Service.ps1` — **PASS** — 332 lines. NSSM primary / sc.exe fallback. `SERVICE_DELAYED_AUTO_START`, restart 10 s / 30 s / 30 s, log rotation 10 MB.
4. `ops/installer/Uninstall-JojoBot.ps1` — **PASS** — 373 lines. Stops + removes service; `-Purge` wipes `%APPDATA%\JojoBot\` + `%ProgramData%\JojoBot\`.
5. `ops/scheduler/Run-DeviceCodeLogin.ps1` — **PASS** — 149 lines. Invokes MSAL device-code re-auth; verifies token cache; Application event-log entries.
6. `src/backend/routers/settings_router.py` — **PASS** — 368 lines. 5 sections + status sidebar. 16 endpoints. `from jojo_core import config`. Mounted at `/api/settings`. `test_settings_endpoints.py` (273 lines) confirmed.
7. `packages/jojo_qa/synthesize.py _call_model` — **PASS** — Live Anthropic SDK confirmed: line 288 `import anthropic`, line 311 `client = anthropic.Anthropic(api_key=api_key)`, line 312–317 `client.messages.create(model=model, max_tokens=2048, ...)`, line 319 `except anthropic.AuthenticationError`. Model map at 290–294: `sonnet`→`claude-sonnet-4-6`, `opus`→`claude-opus-4-7`.
8. `src/frontend/app/welcome/page.tsx` — **PASS** — 208 lines. `useEffect` fetches `/api/ops/status`; `setInterval(fetchStatus, 10_000)`; `STATUS_KEYS.every((key) => data[key].ok)` → `router.push("/")` on all-green.
9. `src/frontend/app/(tabs)/settings/page.tsx` — **PASS** — 189 lines. 5 section components present and imported.
10. `docs/ops/distribution.md` — **PASS** — 232 lines. Full install guide for non-developer staff.
11. `docs/ADR/0013-phase-7b-standalone-workstation-installer.md` — **PASS** — 101 lines. Captures supersession of shared-server plan, DPAPI config, per-install isolation, revisit conditions.
12. `docs/phase-7b-exit-evidence.md` — **PASS** — 93 lines. Per-deliverable table, test results, known gaps, ADR references, conclusion.

---

## Informational Notes (no action required)

- **Item 2 naming:** `ops/installer/Install-JojoBot.ps1` (repo-resident) is the developer bootstrap; the end-user Service-registering script is the heredoc inside `Build-JojoBotRelease.ps1`. Intentional; documented in distribution.md.
- **No `dist/` artifact:** exit criterion says "produced by" — capability claim, not a pre-built artifact claim. PASS.
- **Next.js static export:** build script writes `frontend-build-note.txt` when `rewrites()` prevents static export. Known follow-up; not exit-blocking.

---

## VERDICT

**Phase 7b: PASS** — exit criterion met, ready to proceed to Phase 8.
