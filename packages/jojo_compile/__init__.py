"""jojo_compile — the absorb loop that compiles ``ask_jojo_raw/`` into ``ask_jojo_wiki/``.

Owns raw→wiki compilation: plan, write, verify, link, checkpoint, reorganize.
Phase 2 populates this package. Phase 0 is skeleton only.
"""

__version__ = "0.1.0"
